<header class="w-full p-4 bg-gray-200 flex justify-between">
  <div class="logo"><a href="{{ '/' | url }}" class="font-semibold text-xl">{{ site.meta.title }}</a></div>
  <div class="formBx">
    <form id="searchForm" action="{{ '/search' | url }}" class="flex">
      <input type="text" name="q" id="header-searchbox" class="w-full sm:max-w-xs bg-slate-200 border border-transparent float-right bg-white focus:border-slate-300 focus:outline-none h-8 p-4 placeholder-slate-500 rounded text-slate-700 text-sm mr-4">
      <button>search</button>
    </form>
  </div>
</header>